program Program4;
  var Sum, I, N : Integer;
   t : Float;
   function f1 (var i : boolean; s : String) : String ;
        var m : Integer;
        begin
            if i = True then
                m:= 21
            else
                N:= 99;
        f1:= 'string';

        end ;
  begin

     Read(N);
     Write(f1(I,N,'hello'));
     I   := 0;
     Sum := 0;
     repeat
       Sum := Sum + I;
       I   := I + 1;
       Write(I, Sum);
     until (I > N) and (I <= 100)
     end.